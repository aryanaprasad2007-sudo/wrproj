{
  "name": "daily-docket-pwa",
  "private": true,
  "type": "module",
  "description": "No build step for the app itself — this file exists only so Netlify installs the two npm packages netlify/functions/_push.mjs needs.",
  "dependencies": {
    "web-push": "^3.6.7",
    "@netlify/blobs": "^8.1.0"
  }
}
