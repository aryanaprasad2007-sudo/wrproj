anthropic>=0.75
PyYAML>=6.0
python-dotenv>=1.0

# service
fastapi>=0.115
uvicorn>=0.32
pydantic>=2.9
httpx>=0.27

# voice (phase 2)
piper-tts>=1.3          # local TTS; download a voice model separately
sounddevice>=0.5        # speaker output; falls back to WAV files without it
